import styled from 'styled-components';

/**
 * max-height: ${({ section }) => (section === 'collectionType' ? '800px' : '180px')};
 */

const LeftMenuListLink = styled.div`
  margin-bottom: 19px;
  margin-right: 25px;
  overflow: auto;
`;

export default LeftMenuListLink;
